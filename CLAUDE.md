# VMMO Bot - Важные правила для Claude

## SSH ключи и доступ к серверам

**Локальный SSH ключ (без passphrase):**
- Путь: `~/.ssh/id_ed25519_vmmo`
- Публичный ключ: `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIG6Nk7NGphIN/1McdMCcTIhZ7hzLO24sEWnJkwBJCUu0 vmmo_bot`

**Серверы:**

| Сервер | IP | Пароль root | SSH ключ добавлен | Статус |
|--------|-----|-------------|-------------------|--------|
| Германия (старый) | 45.148.117.107 | JO8d3GSHqvLV | ДА (id_ed25519_vmmo) | Архив |
| Нидерланды (основной) | 45.131.187.128 | 6pCV9qfZiRTH | ДА (id_ed25519_vmmo) | Активный |

**DuckDNS:**
- Домен: `faizbot.duckdns.org`
- Токен: `7c65da64-4f47-4643-b812-6a801ea3605f`
- Обновление IP: `curl "https://www.duckdns.org/update?domains=faizbot&token=7c65da64-4f47-4643-b812-6a801ea3605f&ip=NEW_IP"`

**Подключение:**
```bash
ssh -i ~/.ssh/id_ed25519_vmmo root@45.131.187.128  # Нидерланды (основной)
ssh -i ~/.ssh/id_ed25519_vmmo root@45.148.117.107  # Германия (старый, архив)
```

## Деплой на сервер

**ВАЖНО:** Боты запускаются из `/home/claude/vmmo_bot/`, НЕ из `/root/vmmo_bot/`!

### Порядок деплоя (ОБЯЗАТЕЛЬНО)

```
1. Коммит изменений локально
2. Пуш в GitHub
3. scp файлов на сервер
4. Очистка __pycache__
```

**GitHub репозиторий:** `https://github.com/Hapypopka/vmmo_bot.git`

### Команды деплоя:

```bash
# 1. Коммит и пуш (локально)
git add -A && git commit -m "описание" && git push

# 2. Копирование на сервер
scp -i ~/.ssh/id_ed25519_vmmo <файл> root@45.131.187.128:/home/claude/vmmo_bot/<путь>

# 3. Очистка кэша Python
ssh -i ~/.ssh/id_ed25519_vmmo root@45.131.187.128 "rm -rf /home/claude/vmmo_bot/requests_bot/__pycache__/"
```

### Быстрый деплой всего requests_bot/:
```bash
scp -i ~/.ssh/id_ed25519_vmmo -r requests_bot/*.py root@45.131.187.128:/home/claude/vmmo_bot/requests_bot/
```

## Правила работы

1. **Никогда не запускать ботов самостоятельно** - это делает только пользователь через Telegram
2. **Всегда деплоить изменения на сервер** после редактирования локальных файлов
3. После деплоя пользователь должен перезапустить ботов через Telegram
4. **Не забывать деплоить** - если изменил код локально, сразу залить на сервер
5. **НИКОГДА не деплоить папку profiles/** - настройки персонажей хранятся ТОЛЬКО на сервере и редактируются через Telegram бота. Локальные конфиги устарели!
6. **Коммиты на русском языке** - чтобы было понятно что изменилось
7. **После коммита всегда деплоим** - коммит + пуш + scp на сервер

## Что деплоить, а что НЕТ

| Папка/файл | Деплоить? | Причина |
|------------|-----------|---------|
| `requests_bot/*.py` | ✅ ДА | Код бота |
| `requests_bot/templates/` | ✅ ДА | Шаблоны веб-панели |
| `requests_bot/static/` | ✅ ДА | Статика веб-панели |
| `profiles/*/config.json` | ❌ НЕТ | Настройки на сервере актуальнее! |
| `profiles/*/cookies.json` | ❌ НЕТ | Сессии персонажей |
| `profiles/*/deaths.json` | ❌ НЕТ | Статистика смертей |
| `profiles/*/status.json` | ❌ НЕТ | Текущий статус |
| `CLAUDE.md`, `ISSUES.md` | ❌ НЕТ | Только локальная документация |

## Структура проекта

```
warrior/                   # Локальная разработка
├── .mcp.json              # MCP конфиг для браузера
├── auction_blacklist.json # Чёрный список аукциона
├── CLAUDE.md              # Этот файл - главный контекст
├── CRAFT_REQUIREMENTS.md  # Требования крафта сета
├── ISSUES.md              # Найденные проблемы
├── MINING_RECIPES.md      # Рецепты горного дела
├── mcp_browser/           # MCP сервер для браузера
│   ├── requirements.txt
│   └── server.py
├── profiles/              # Локальные профили (6 шт, устаревшие)
└── requests_bot/          # Весь код бота
    ├── static/            # CSS/JS веб-панели
    └── templates/         # HTML шаблоны
```

### Профили на сервере (21 шт)
```
char1-char21 - все боты работают параллельно
```

**Сервер:** `root@45.131.187.128` (Нидерланды)
**GitHub:** `https://github.com/Hapypopka/vmmo_bot.git`

## Сложности данжей (URL параметры)

При формировании URL для прямого входа в данжен:
- `1=impossible` = Брутал
- `1=hard` = Героик
- `1=normal` = Нормал

Пример: `/dungeon/lobby/NYLairFrost_2026?1=impossible`

Внутри кода используем `brutal`/`hero`/`normal` для логики и UI парсинга.

## Ивент-данжены (EVENT_DUNGEONS в event_dungeon.py)

Все ивенты проходятся автоматически, если `ny_event_dungeon_enabled: true` в конфиге профиля.

| Ключ | Название | Сложность |
|------|----------|-----------|
| NYLairFrost_2026 | Логово Демона Мороза | Брутал |
| SurtCaves | Пещеры Сурта | Нормал |
| DarknessComing | Предвестник Тьмы | Брутал |
| NYIceCastle_2026 | Ледяная Цитадель | Брутал |

**Исключён:** Лабиринт (mirrormaze) - требует особой логики.

КД ивентов парсится с сервера после победы (не хардкожен).

## Защищённые предметы (PROTECTED_ITEMS в config.py)

Эти предметы НЕ продаются и НЕ разбираются:

**Крафт:**
- Железо, Железная Руда, Железный Слиток
- Медь, Медная Руда, Бронза
- Платина

**Квестовые/ценные:**
- Треснутый Кристалл Тикуана
- Печать Сталкера I/II/III
- Золотой Оберег
- Изумительная пылинка
- "Оберегов" (частичное совпадение - все ларцы)

**Сундуки (открываются, не продаются):**
- Сундук, Ларец, Ящик, Шкатулка

**Экипировка:**
- Шлем Нордов

**Ресурсы ивентов:**
- Ледяной Кристалл
- Уголь Эфирного Древа

## Ключевые механики бота

### Проверка крафта
`try_restart_craft()` вызывается:
- Перед очисткой рюкзака
- Перед сбором почты
- Перед каждым данженом
- Перед ивент-данженами

### Черный список аукциона
Файл: `auction_blacklist.json`
- Предметы добавляются если нет конкурентов или цена слишком низкая
- Такие предметы разбираются, а не продаются

### Вкладки данжей (dungeon_tabs в конфиге)
- `tab2` = 50+ уровень (по умолчанию)
- `tab3` = 30-39 уровень
- `extra_dungeons` = дополнительные данжены из других вкладок

### Режимы крафта (craft_mode в конфиге)
- `iron` - крафт железа
- `bronze` - крафт бронзы
- `platinum` - крафт платины

## Основные файлы (requests_bot/)

| Файл | Назначение |
|------|------------|
| **Ядро** | |
| bot.py | Главный цикл бота |
| client.py | HTTP клиент (requests) |
| config.py | Конфигурация, защищённые предметы |
| logger.py | Логирование |
| **Бой** | |
| combat.py | Логика боя, сбор лута |
| run_dungeon.py | Прохождение обычных данжей |
| event_dungeon.py | Ивент-данжены |
| hell_games.py | Адские Игры |
| survival_mines.py | Заброшенная Шахта |
| arena.py | PvP арена (дуэли) |
| **Инвентарь** | |
| backpack.py | Рюкзак, открытие бонусов |
| auction.py | Аукцион, продажа, разборка |
| mail.py | Почта - сбор писем и золота |
| **Крафт и ресурсы** | |
| craft.py | Крафт (железо/бронза/платина) |
| craft_prices.py | Цены крафта |
| sell_resources.py | Продажа ресурсов |
| sell_crafts.py | Продажа крафтовых предметов |
| resources.py | Трекер ресурсов по сессиям |
| resource_history.py | История ресурсов |
| **Утилиты** | |
| gold_transfer.py | Передача золота между персонажами |
| daily_rewards.py | Ежедневные награды |
| pets.py | Питомцы |
| popups.py | Обработка попапов |
| watchdog.py | Watchdog для мониторинга |
| stats.py | Статистика |
| **Интерфейсы** | |
| telegram_bot.py | Телеграм управление |
| web_panel.py | Веб-панель (Flask) |
| **Создание персонажей** | |
| create_character.py | Создание персонажа (требует node.js) |
| tutorial.py | Прохождение туториала |

## Веб-панель (Mini App)

**URL:** https://faizbot.duckdns.org
**Пароль:** `1616`

### Функции веб-панели:
- Управление ботами (запуск/стоп/рестарт)
- Управление Telegram ботом (запуск/стоп/рестарт)
- Просмотр статистики и ресурсов
- Настройка профилей
- Защищённые предметы (добавление/удаление)
- Просмотр инвентаря крафта
- AI Debug (Claude)
- Сброс скипов данжей

### Telegram Mini App
Кнопка "🌐 Веб-панель" в TG боте открывает веб-панель как Mini App внутри Telegram.

**Автологин:** При открытии через Telegram Mini App - автоматический вход без пароля (проверка через initData).

### Технические детали:
- Flask + Jinja2 templates
- HTTPS через nginx + Let's Encrypt
- Домен: faizbot.duckdns.org (DuckDNS, бесплатный)
- SSL сертификат автообновляется через certbot

### Перезапуск веб-панели:
```bash
ssh -i ~/.ssh/id_ed25519_vmmo root@45.131.187.128 "fuser -k 5000/tcp; sleep 1; cd /home/claude/vmmo_bot && nohup python3 -m requests_bot.web_panel > /tmp/web_panel.log 2>&1 &"
```

### Файлы веб-панели:
- `requests_bot/web_panel.py` - Flask приложение
- `requests_bot/templates/` - HTML шаблоны
- `requests_bot/static/` - статические файлы (CSS, JS)

### HTML шаблоны (templates/):
| Файл | Назначение |
|------|------------|
| base.html | Базовый шаблон с навигацией |
| index.html | Главная - управление ботами |
| login.html | Страница авторизации |
| stats.html | Статистика ресурсов |
| config.html | Настройки профиля |
| logs.html | Логи бота |
| deaths.html | Смерти и сложность данжей |
| profiles.html | Управление профилями |
| protected.html | Защищённые предметы |
| inventory.html | Инвентарь крафта |
| ai.html | AI Debug (Claude) |

### API Endpoints:
**Боты:**
- `POST /api/bot/<profile>/start` - запуск бота
- `POST /api/bot/<profile>/stop` - остановка
- `POST /api/bot/<profile>/restart` - перезапуск
- `POST /api/bot/start_all` - запустить всех
- `POST /api/bot/stop_all` - остановить всех

**Telegram бот:**
- `GET /api/telegram_bot/status` - статус
- `POST /api/telegram_bot/start|stop|restart`

**Конфиг:**
- `GET /api/config/<profile>` - получить
- `POST /api/config/<profile>` - сохранить
- `POST /api/config/<profile>/toggle/<setting>` - переключить

**Данные:**
- `GET /api/stats` - вся статистика
- `GET /api/logs/<profile>` - логи
- `GET /api/deaths/<profile>` - смерти
- `POST /api/deaths/<profile>/reset` - сбросить
- `GET /api/inventory` - инвентарь крафта

**Профили:**
- `GET /api/profiles` - список
- `POST /api/profiles/create` - создать
- `POST /api/profiles/<profile>/delete` - удалить

**Защищённые предметы:**
- `GET /api/protected` - список
- `POST /api/protected/add` - добавить
- `POST /api/protected/remove` - удалить

**Утилиты:**
- `POST /api/reset_skips` - сброс скипов данжей
- `POST /api/ai/ask` - запрос к Claude

## MCP Browser (mcp_browser/)

MCP сервер для управления браузером через Claude Code.

### Дефолтный аккаунт (для исследований)
```
Логин: Castertoyi
Пароль: Agesevemu1313!
```
**ВАЖНО:** Использовать этот аккаунт по умолчанию. За чаров (char1-21) логиниться только по запросу пользователя!

### Доступные инструменты:
| Инструмент | Описание |
|------------|----------|
| `browser_login` | **Залогиниться** (по умолчанию Castertoyi) |
| `browser_navigate` | Переход на URL |
| `browser_get_html` | Получить HTML страницы |
| `browser_get_text` | Получить текст |
| `browser_click` | Клик по элементу |
| `browser_fill` | Ввод текста |
| `browser_screenshot` | Скриншот |
| `browser_execute_js` | Выполнить JS |
| `browser_wait` | Ожидание элемента |
| `browser_load_profile` | Загрузить профиль (char1-21) - только по запросу! |
| `browser_info` | Информация о странице |

---

## VPN (Reality/VLESS)

**x-ui панель:** http://45.131.187.128:15619

VPN работает на порту **8443** (не 443, т.к. 443 занят nginx для HTTPS).

### VPN пользователи (все на порту 8443):
| Имя | UUID | subId |
|-----|------|-------|
| Faiz (main) | 4678b413-dde6-4a9f-b57c-e154633f0f6f | reality123 |
| Ильмир | 15c47bf3-ea1d-418b-afa0-fc8eea553276 | ilmir456 |
| Faiz-2 | f4a2fda4-6763-4c7e-a560-ca60a96a3bb5 | faiz789 |
| Зульфия | 7d7b4c50-11fc-4df1-92d2-c24b8ca0a0a6 | lgfdsox2c1gl0s5j |
| Фаиз | e1c5fb07-f05f-4edf-a5fc-ab3812f77631 | veh6hfbsafmgupfj |
| Ильгам | 84ec4d41-a4b8-40e8-851f-d242c86dc207 | zox9snw9u8lve2dw |
| ПК Ильгам | d241b4cc-5001-4919-a267-50e1ac1adb85 | hk8gfmtqpggmv6mz |

### Ссылка для подключения (Hiddify/v2rayNG):
Формат ссылки (подставить нужный UUID):
```
vless://UUID@45.131.187.128:8443?type=tcp&security=reality&pbk=78Men0MPTAojbX2S7SL2n9OmUh3HPuj35uRmaH_kTFw&fp=chrome&sni=www.microsoft.com&sid=fc0f1ebc97ae1e55&spx=%2F&flow=xtls-rprx-vision#ИМЯ
```

### Управление x-ui:
```bash
systemctl restart x-ui   # перезапуск
systemctl status x-ui    # статус
```

### Watchdog (автопроверка каждые 5 мин):
Скрипт `/usr/local/bin/check-xray.sh` проверяет что порт 8443 слушается и перезапускает x-ui если нет.
Логи: `/var/log/xray-watchdog.log`

```bash
# Проверить лог watchdog
cat /var/log/xray-watchdog.log
```

### Если нужно изменить порт VPN:
```bash
sqlite3 /etc/x-ui/x-ui.db "UPDATE inbounds SET port = НОВЫЙ_ПОРТ WHERE id = 8;"
systemctl restart x-ui
```

## Порты на сервере

| Порт | Сервис |
|------|--------|
| 80 | nginx (редирект на 443) |
| 443 | nginx (HTTPS веб-панель) |
| 5000 | Flask веб-панель (internal) |
| 8443 | xray Reality VPN |
| 15619 | x-ui веб-панель |
| 2096 | x-ui subscription server |

## Технические детали кода

### Wicket AJAX Framework
Игра использует Apache Wicket с паттерном `Wicket.Ajax.get({u:'URL'})`.
- Парсинг через regex: `Wicket\.Ajax\.ajax\(\{[^}]*"c":"([^"]+)"[^}]*"u":"([^"]+)"`
- Заголовки для AJAX: `Wicket-Ajax: true`, `Wicket-Ajax-BaseURL`, `X-Requested-With`

### Metronome (Heartbeat)
**Критично для сбора лута!** Интервал 1.5 сек.
- Без heartbeat лут не собирается автоматически
- URL парсится из `Wicket.Timer.set('metronome', ...)`

### Combat System
- Enemy positions: 21-25
- Skill positions: 1-5
- GCD = 2.0 сек
- ATTACK_CD = 0.6 сек
- Дефолтные КД скиллов: `{1: 15.5, 2: 24.5, 3: 39.5, 4: 54.5, 5: 42.5}`

### Lock файлы
`profiles/<char>/.lock` - защита от дублей процессов.
Формат: `PID|timestamp`

### Status файлы
`profiles/<char>/status.json` - текущая активность бота.
Обновляется через `set_activity()` для отображения в TG боте.

### Deaths.json
`profiles/<char>/deaths.json` - счётчик смертей по данженам.
Используется для понижения сложности: brutal → hero → normal → skip

## Создание персонажа (create_character.py)

**ВАЖНО:** Требуется **Node.js** на машине!

### Полный поток создания:
1. Главная `/` -> Нажать "Начать игру" -> `/training/battle` (лоадер)
2. Извлечь функцию `ppt()` и аргумент из HTML
3. Выполнить `ppt(arg)` через **node.js** -> получить `ppKey`
4. Вызвать `ppAction=go&ppKey=...` -> получить `urlStart`
5. Вызвать `urlStart&sex=m&side=1&type=1` -> персонаж создан -> таверна

### Почему нужен node.js:
Функция `ppt()` генерируется сервером с **уникальными константами для каждой сессии**. Нельзя захардкодить - нужно выполнять динамически.

### Ключевые находки:
- **URL страницы может быть `/training/battle`**, но контент - страница выбора класса! Проверять по **контенту**, не по URL
- После создания персонаж попадает в **ТАВЕРНУ**, не в бой
- Заголовок `ptxSPA: true` нужен для SPA-навигации
- `Ptx.Shadows.Tutor.urlStart` содержит базовый URL для создания
- Без `ppAction=go` сначала - получаем `accessDenied`

### Параметры создания:
| Параметр | Значения |
|----------|----------|
| sex | m (муж), f (жен) |
| side | 1 (свет), 2 (тьма) |
| type | 1 (Маг), 2 (Монах), 3 (Воин) |

## Туториал (tutorial.py)

### Маппинг квестов на данжены:
**ВАЖНО:** ID данженов отличаются от ID квестов!

| Квест | Данжен | Особенность |
|-------|--------|-------------|
| new_tuturial1_quests_1_1 | d_new_tuturial_quests_1_1 | Просто бой |
| new_tuturial2_quests_1_1 | d_new_tuturial_quests_1_5 | Использовать скилл |

### API таверны:
- `apiQuestViewUrl` - просмотр квеста (POST с quest_id)
- `apiQuestAcceptUrl` - принять квест
- `apiQuestCompleteUrl` - сдать квест

### Проверка состояния:
Признаки что мы в таверне:
- `Ptx.Shadows.Ui.Tavern` в HTML
- `apiQuestViewUrl` в HTML
- `Таверна` в title

## Исправленные баги

### Баг accessDenied при создании персонажа (2026-01-14)
**Проблема:** Сервер возвращал `accessDenied` при прямом запросе к `/user/customize?ppAction=select`.

**Причина:** Поток создания требует предварительного вызова `ppAction=go` с динамическим `ppKey`. Функция `ppt()` генерируется с уникальными константами для каждой сессии.

**Решение:**
1. Извлекаем `ppt()` и аргумент из HTML
2. Выполняем через node.js: `subprocess.run(['node', '-e', js_code])`
3. Вызываем `ppAction=go&ppKey=...`
4. Получаем `urlStart` и создаём персонажа

### Баг неработающего скилла в туториале (2026-01-14)
**Проблема:** Второй туториальный квест `new_tuturial2_quests_1_1` не сдавался (status: Fail). Требуется использовать скилл "Гром и Молния" в бою.

**Причина:** Метод `_find_skill_url()` искал паттерн `"c":"ptx_combat_rich2_skill_link_1"`, но HTML содержит другой формат: `"u":"...skills-0-skillBlock-skillBlockInner-skillLink..."`.

**Решение:** Добавлен новый паттерн поиска URL скилла по `skills-{index}-skillBlock-skillBlockInner-skillLink`. Также исправлен DEFAULT_BASE_URL с `vmmo.vten.ru` на `m.vten.ru` т.к. создание персонажа происходит на мобильном домене.

### Баг accessDenied в туториале (2026-01-14)
**Проблема:** TutorialRunner получал `accessDenied` при переходе в таверну после создания персонажа.

**Причина:** Создание персонажа происходит на `m.vten.ru`, куки привязаны к этому домену. TutorialRunner использовал `DEFAULT_BASE_URL = "https://vmmo.vten.ru"` - другой домен, куки не передаются.

**Решение:** Изменён `DEFAULT_BASE_URL` на `https://m.vten.ru` в `tutorial.py`.

### Баг "vmmo.vten.runone" в craft.py (2026-01-13)
**Проблема:** Крафт бронзового слитка падал с ошибкой DNS `vmmo.vten.runone`.

**Причина:** В `start_craft()` условие `level == 2` не покрывало бронзовый слиток (level=3). Код шёл в обычный путь где `f"{BASE_URL}{recipe['start_url']}"` склеивал домен с `None` → `"https://vmmo.vten.ruNone"`.

**Решение:** Изменено `level == 2` на `level >= 2` для всех рецептов 2+ уровня. Также добавлена валидация URL перед `urljoin()` во всех местах.

### Баг квест экипировки не сдаётся (2026-01-14)
**Проблема:** Третий туториальный квест `new_tuturial1_quests_1_2` (Основы экипировки) возвращал 404 при сдаче, хотя предмет был надет.

**Причина:** Код предполагал что `next_quest_id` из Complete API означает что квест автоматически принят. На самом деле это только **информация** о следующем квесте - сам квест НЕ принят! Сервер показывал `status=none, progress={current:0, total:1}`.

**Решение:**
1. Специальные квесты (equip_item, upgrade_amulet) ВСЕГДА требуют явного вызова `_accept_quest()` перед выполнением действия
2. Добавлена проверка статуса квеста через `apiFullUrl` перед попыткой Complete
3. Изменена логика: `if is_special or not next_quest_id: _accept_quest()`

**ВАЖНО для Claude:** `next_quest_id` из API - это только подсказка какой квест следующий, НЕ факт что он принят! Всегда проверять реальный статус через `apiFullUrl` или явно принимать квест.

### Баг "Работает: X ч" не сбрасывается после рестарта (2026-01-27)
**Проблема:** После перезапуска ботов у некоторых в веб-панели показывалось старое время работы (например "0.4 ч" вместо сброса).

**Причина:** `check_craft()` вызывался в `login()` ДО инициализации ресурсов в `run()`. При этом `check_craft()` переходил на страницы крафта, после чего `open_backpack()` мог вернуть не ту страницу, `parse_resources()` возвращал None и `start_session()` никогда не вызывался. В итоге `start_time` в resources.json оставался от предыдущей сессии.

**Решение:**
1. Создан метод `_init_resources_session()` в bot.py
2. Вызов перенесён в `login()` ПЕРЕД `check_craft()`
3. Удалён дублирующийся код из `run()`

**ВАЖНО для Claude:** Порядок инициализации критичен! Ресурсы должны парситься ДО любых действий, которые могут изменить текущую страницу.

## Найденные проблемы

См. файл `ISSUES.md` - документация всех найденных проблем при аудите.

**Статистика:** 1 критичная, 5 средних, 30 minor проблем.

---

## Правило для Claude: Автообновление контекста

**ВАЖНО:** После каждого значимого изменения Claude ДОЛЖЕН обновить этот файл:

1. **Нашёл баг** → Добавить в "Исправленные баги" с датой, проблемой, причиной, решением
2. **Изменил логику модуля** → Обновить соответствующий раздел
3. **Нашёл важный нюанс** → Добавить в раздел модуля или создать новый
4. **Добавил новый файл** → Добавить в таблицу "Основные файлы"
5. **Изменил API/endpoints** → Обновить раздел API

**Формат записи бага:**
```
### Баг <краткое описание> (<дата>)
**Проблема:** Что происходило
**Причина:** Почему происходило
**Решение:** Что сделали
```

---

## История изменений

### 2026-01-23 - Полная очистка проекта
**Локально:**
- Удалён старый Playwright бот (16 файлов в корне)
- Удалены debug/test файлы (~35 файлов)
- Удалены сохранённые веб-страницы (5 .htm + папки)
- Удалены папки: archive/, baby_keyboard/, create/, quest_explorer/, logs/
- Удалены устаревшие контексты
- Удалены неиспользуемые: websocket_loot.py, main.py, check_inventory.py

**На сервере:**
- Удалён Playwright мусор из корня
- Удалены debug HTML и тестовые файлы из requests_bot/
- Актуализирована информация: 21 профиль (char1-char21)

**Добавлено правило:** Всегда коммитить и пушить перед деплоем
